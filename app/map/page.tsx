import FindApp from "@/components/find/find-app";

export const metadata = {
  title: "Free food near you — map",
  description: "Find food pantries, distribution events, and programs you can apply to.",
};

export default function Page() {
  return <FindApp />;
}
